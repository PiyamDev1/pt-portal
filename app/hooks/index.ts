/**
 * App-Local Hooks Barrel Export
 * 
 * Compatibility layer for backward compatibility.
 * All shared hooks have been migrated to root hooks/ and are re-exported here.
 * New imports should use @/hooks instead of @/app/hooks
 * 
 * @module app/hooks
 * @deprecated Use @/hooks instead
 */

export * from '@/hooks'
